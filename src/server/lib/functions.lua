--[[

定义服务端公用函数

]]

function getCityId(uid)
    return string.format(CITY_ID_KEY, uid)
end

function getStrongholdId(uid)
    return string.format(STRONGHOLD_ID_KEY, uid)
end

function getCityMapId(uid)
    return string.format(CITY_MAP_ID_KEY, uid)
end

function getWallsId(uid)
    return string.format(WALLS_ID_KEY, uid)
end

function getStorehouseId(uid)
    return string.format(STOREHOUSE_ID_KEY, uid)
end

function getWatchtowerId(uid)
    return string.format(WATCHTOWER_ID_KEY, uid)
end

function getHospitalId(uid)
    return string.format(HOSPITAL_ID_KEY, uid)
end

function getBarrackId(uid)
    return string.format(BARRACK_ID_KEY, uid)
end

function getEmbassyId(uid)
    return string.format(EMBASSY_ID_KEY, uid)
end

function getMarketplaceId(uid)
    return string.format(MARKETPLACE_ID_KEY, uid)
end

function getTavernId(uid)
    return string.format(TAVERN_ID_KEY, uid)
end

function getAcademyId(uid)
    return string.format(ACADEMY_ID_KEY, uid)
end

function getBlackSmithId(uid)
    return string.format(BLACKSMITH_ID_KEY, uid)
end

function getFarmId(uid)
    return string.format(FARM_ID_KEY, uid)
end

function getLoggingCampId(uid)
    return string.format(LOGGINGCAMP_ID_KEY, uid)
end

function getMineId(uid)
    return string.format(MINE_ID_KEY, uid)
end

function getQuarryId(uid)
    return string.format(QUARRY_ID_KEY, uid)
end

function getWaterWheelId(uid)
    return string.format(WATERWHEEL_ID_KEY, uid)
end

